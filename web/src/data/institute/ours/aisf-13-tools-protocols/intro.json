{
  "blurb": "A model that can only talk is a demo; a model that can call tools, safely, is useful. Function calling, the Model Context Protocol, Agent Skills, and the security around them are how an assistant for the Panth actually reads a calendar, searches an archive, or reserves a langar slot."
}
