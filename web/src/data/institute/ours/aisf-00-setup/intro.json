{
  "blurb": "Every tool in this building assumes you can open a terminal, run code, and read an error without flinching. Get that footing first — the browser lab lets you start today and install locally only once a real project needs it. The sooner setup stops being the obstacle, the sooner you are building something the sangat can use."
}
