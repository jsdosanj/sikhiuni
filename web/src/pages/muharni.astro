---
// The Muharni now lives inside the expanded Baal Updesh primer.
const dest = '/baal-updesh#muharni';
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="refresh" content={`0; url=${dest}`} />
    <link rel="canonical" href={dest} />
    <title>Muharni → Baal Updesh, Sikhi University</title>
    <script set:html={`location.replace(${JSON.stringify(dest)})`} />
  </head>
  <body>
    <p>The Muharni is now part of <a href={dest}>Baal Updesh</a>. Redirecting…</p>
  </body>
</html>
