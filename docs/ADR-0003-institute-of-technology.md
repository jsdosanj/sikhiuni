# ADR-0003: Sikhi University · Institute of Technology (the engineering wing)

**Status:** Accepted · **Date:** 2026-08-28 · **Plan:** `WORLD-CLASS-IOT-PLAN.md` (four gstack reviews cleared)

## Context
Sikhi youth worldwide already use and trust sikhiuni.com. There is no path inside it to the
software / AI-engineering skills that pay. freeCodeCamp / boot.dev / "AI Engineering from
Scratch" teach this well and free — so the content is not the differentiator.

## Decision
Add a **namespaced wing at `/institute/*`** — "Sikhi University · Institute of Technology" —
with its own dark, retro-futurist design system (`DESIGN-INSTITUTE.md`), an in-browser code
lab, and a curriculum built from **openly-licensed sources** plus our own additions.

**The wedge (why us):** the Sikh community's own institution now teaches the skills that pay,
in the community's voice, and points graduates at *building for the Panth* (a capstone of
real community tools). That is the part freeCodeCamp structurally cannot have.

**Ship shape:** a *Full Minimum Lovable Institute* — AISF phases 0, 2, 3, 7, 11, 13, 14
(~149 lessons), the sikhi.io coding courses, the guide, and the capstone. The other 13 AISF
phases, the Open Source Atlas, and two more explore booths are deferred to depth waves after
a cohort soft-launch.

### Amendment 1 — sub-schools (2026-09-09)
The wing is split into **named sub-schools**, declared once in `manifest.schools` and
carried on every track as a `school` field:

- **AI School** (`ai`) — everything that already existed: the 20 AISF phases, both Sikh Code
  dojos, the Claude Code guide, the "Build for the Panth" capstone. No content change.
- **Cybersecurity School** (`cyber`) — new. Six `kind: "path"` tracks: foundations, blue team,
  red team, the range, cloud security, and "Cyber for Schools & Families" for Khalsa-school
  staff and gurdwara youth programmes.
- **School of IT** (`it`) — new. One path: IT Support, a reading map over Apple Professional
  Training and Microsoft Learn, ending in credentials employers filter on.

**Why a new track kind rather than reusing `phase`.** A `phase` owns its lesson bodies
(`imported/<track>/`) and can carry a server-graded exam. A `path` owns *no* lesson bodies at
all: its unit is a **module** — our objective and teaching note — ending in links to free labs
hosted by whoever runs them. That distinction is the licensing posture made structural, so
"we rehost the security labs" cannot happen by accident; `validate-institute.mjs` fails the
build if a lab href is anything but an absolute `https` link out (or an internal
`/technology/` route). Consequence: paths carry **no exam and no certificate** — the
credential lives with the range that graded you, not with us.

Routes added: `/technology/school/<slug>` for each school; the hub, catalogue, and sitemap
group by school. Every school uses the one `--filament` accent (D2 reserves `--signal` for
live/correct states) and they are told apart by a monospace mark and label.

**Cost vocabulary is a fixed enum, not free text** (`COSTS` in `validate-institute.mjs`). A
learner deciding what they can afford is the reader least able to tolerate vague copy, so
"free" may not be stretched over a free course with a paid exam behind it
(`free course · paid exam`) or a self-hosted lab that bills the student's own cloud account
while it runs (`free · your cloud bill`). The Cloud Security path also carries a standing
`warning` above its modules: those labs are vulnerable by design, so an abandoned one is both
a bill and somebody else's foothold.

**Not every path adapts its source.** `creditLabel` sets the verb on the attribution line —
"Sequencing adapted from" for the CC0 roadmap, "Labs discovered through" for the unlicensed
cloud directory, "Courses published by" for the vendor training. Getting that verb wrong would
claim a licence we do not have.

## Architecture
- **Same spine, extended, not forked.** `Base.astro` gains an `institute` prop (fonts,
  `body[data-institute]`, translate opt-out, dark-default). `web/src/styles/institute.css`
  is scoped entirely under `body[data-institute]` — inert on the rest of the site.
- **Data:** `web/src/data/institute/` — `manifest.json` (the spine) + `imported/<track>/`
  (from AISF, regenerated by `scripts/sync-aisf.mjs`, committed) + `ours/<track>/` (our seva
  intros, lab checks — a re-sync never touches these). Lesson JSON ships as static assets
  (~5 MB, under the Cloudflare limit — no R2 for the MLI).
- **Quizzes:** server-graded by `/api/quiz` against `functions/api/_institute-quiz-keys.js`
  (generated, server-only, CI diff-gated). Same anti-forgery guarantee as the main catalogue.
- **The code lab:** a lean `<textarea>` editor (not CodeMirror — the strict `style-src`),
  JS in a same-origin Web Worker (10 s `terminate()` cap), HTML in a
  `<iframe sandbox="allow-scripts">` with checks run in-frame. Python via Pyodide is a later
  wave.
- **CSP:** `wrangler.toml` `run_worker_first = ["/institute", "/institute/*"]`; `worker.js`
  widens the asset layer's own hash-hardened CSP for those paths only —
  `'wasm-unsafe-eval'` + jsDelivr (Pyodide, mermaid) in `script-src`/`connect-src`,
  `frame-src 'self'` (the preview iframe). The rest of the site is unchanged.
- **Booths** (`/institute/explore`): link-out only, each skinned to its source, no logos, a
  "not affiliated" line. freeCodeCamp for the MLI; SANS and NICCS · CISA added with the
  Cybersecurity School.
- **Paths** (Amendment 1): `data/institute/paths/<track>.json` — modules (objective, teaching
  note, labs) plus a `bench` shelf of reference links. Nothing is fetched or mirrored at build
  time; the file holds only our prose and other people's URLs.

## Licensing (see `docs/CONTENT-AND-LICENSING.md` and `/institute/licenses`)
- **AISF** — MIT, © Rohit Ghumare. Retained MIT text; Rohit credited as professor on every
  AISF phase; our reformatting is ours, curriculum stays MIT.
- **Our additions** (seva intros, lab exercises, capstone briefs, the licenses page) — CC BY-SA 4.0.
- **freeCodeCamp** — curriculum CC BY-SA 4.0, platform BSD-3-Clause. Booth links out,
  rehosts nothing, uses no logo, carries the non-affiliation + trademark notice.
- **Deferred (Libre.academy)** — its packs derive from CC BY-NC / BY-NC-ND books; honoring
  non-commercial / no-derivatives is a hard rule, so that section links out.

## Tradeoffs (explicit)
- ✅ A real skills pipeline for the community, in a trusted platform, built on lawful sources.
- ➖ Large permanent maintenance surface (149+ lessons, two engines, sync pipelines).
- ➖ "Premium" depends on the first ~10 lessons getting real editorial polish — budgeted (C5).
- The credential is a *completion record*, verifiable at `/verify`, with no implied industry
  recognition.

## Consequences
- `/institute/*` stays `noindex` until a cohort soft-launch validates the wedge (C6), then
  it is promoted from the homepage and added to the sitemap.
- AISF updates are re-pulled by `scripts/sync-aisf.mjs`; our `ours/` sidecars survive.
